/** Mesma entrada de tela do app do aluno — ver app/(aluno)/template.tsx. */
export default function ProfessorTemplate({ children }: { children: React.ReactNode }) {
  return <div className="flex flex-1 animate-entrar flex-col">{children}</div>;
}
